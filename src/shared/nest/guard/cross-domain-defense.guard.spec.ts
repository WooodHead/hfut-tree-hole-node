import { CrossDomainDefenseGuard } from './cross-domain-defense.guard'

describe('CrossDomainDefenseGuard', () => {
  it('should be defined', () => {
    expect(new CrossDomainDefenseGuard()).toBeDefined()
  })
})
